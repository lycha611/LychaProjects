<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".HomeActivity">


    <item
        android:id="@+id/action_add_image"
        android:icon="@drawable/ic_add_photo"
        android:showAsAction="ifRoom"
        android:title="@string/action_add_image"
        />

      <item
          android:id="@+id/action_save_beer"
          android:icon="@drawable/ic_floppy_disk"
          android:showAsAction="ifRoom"
          android:title="@string/action_save_beer"
          />

</menu>
